services:
  sophia-proxy:
    image: nginx:1.27-alpine
    container_name: sophia-proxy
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - net1

networks:
  net1:
    external: true
